version: '3.8'

# Production Cloud Storage configuration
# In production, you would connect to actual Google Cloud Storage service
# This file is a placeholder for future production configuration

services:
  # No local emulator in production
  # Application should use real Cloud Storage service with proper credentials
  dummy:
    image: alpine
    command: echo "Production uses real Google Cloud Storage"
